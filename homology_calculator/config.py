CODETYPE = "sig" #["sig", "dt"] currently supported
CONVENTION = "regina" #["regina", "katlas"] currently supported

# NOTES
# Convention: from rough testing, seems like regina and katlas jones polynomials seem to be equivalent up to plugging in q^{-1}. Just lovely.
# As such, since this was created with regina, would highly recommend using "regina" convention, since this is what I used for debugging during development
# in other words, use "katlas" convention at your own risk =D